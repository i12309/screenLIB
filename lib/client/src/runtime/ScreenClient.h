#pragma once

namespace screenlib::client {

// TODO: runtime клиента экрана (client-side) поверх ITransport + ScreenBridge.
class ScreenClient {
public:
    void tick() {}
};

}  // namespace screenlib::client

