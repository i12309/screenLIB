// screenLIB — библиотека. Этот main.cpp существует только для того,
// чтобы env:esp32dev мог собираться как smoke check: проверяет
// компилируемость и линковку lib/core + lib/host под toolchain ESP32.
// Полноценные примеры использования живут в проектах-потребителях
// (Screen32, machine32), а не здесь.

#include <Arduino.h>

void setup() {}
void loop() {}
